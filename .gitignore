# IDE
.idea/
.vscode/
*.iml
*.iws
*.ipr

# Java
target/
*.class
*.jar
*.war

# Node
node_modules/
.next/
dist/
.turbo/

# Python
__pycache__/
*.pyc
*.pyo
.env
venv/
.venv/

# Docker
docker/data/

# Deploy transfer artifact
# /opt/komo has no .git (GitHub is unreachable from the server), so code
# updates go through `git archive` + scp, which drops this tarball at the
# repo root on every deploy.
komo.tar.gz

# Docs (working files, not product)
docs/

# OS
.DS_Store
Thumbs.db

# Internal / Claude
.superpowers/
.claude/
.codex/
CLAUDE.md
AGENTS.md

# Environment
.env
.env.local
*.log
